@model SAPPub.Web.Areas.Profiles.ViewModels.KS2.AcademicPerformanceMeetingOrExceedingStandardsViewModel
@using SAPPub.Web.Constants
@using static SAPPub.Web.Constants.PageTitleConstants
@using SAPPub.Web.ViewComponents.VerticalNavigation

@{
    ViewData["Header"] = "Academic performance";
    ViewData["SubHeader"] = PageTitleConstants.PrimarySchoolPageTitles.MeetingOrExceedingStandards;
    ViewData["Title"] = $"{Model.SchoolName} - Primary {ViewData["SubHeader"]}";
}

<div class="govuk-grid-row">
    @await Component.InvokeAsync("VerticalNavigation",
    new VerticalNavigationModel
    {
        URN = Model.URN,
        SchoolName = Model.SchoolName,
        ActivePage = RouteConstants.PrimaryAcademicPerformanceMeetingOrExceedingStandards,
        IsKS2 = Model.IsKS2,
        IsKS4 = Model.IsKS4,
        IsKS5 = Model.IsKS5
    })

    <div class="govuk-grid-column-three-quarters">
        <span id="school-name-caption" class="govuk-caption-xl">@Model.SchoolName</span>
        <h1 class="govuk-heading-xl">@ViewData["Header"]</h1>
        <p class="govuk-body">
            Looking at school performance data is one way to see how well a school is doing.
            You should look at a range of information about a school before deciding if it’s the right fit for your child.
        </p>

        <nav id="sub-navigation-academic-performance" class="moj-sub-navigation" aria-label="Sub navigation">
            <ul class="moj-sub-navigation__list">
                <li class="moj-sub-navigation__item">
                    <a class="moj-sub-navigation__link" asp-route="@RouteConstants.PrimaryAcademicPerformancePupilProgress" asp-all-route-data="@Model.RouteAttributes">@PrimarySchoolPageTitles.PupilProgress</a>
                </li>
                <li class="moj-sub-navigation__item">
                    <a class="moj-sub-navigation__link" aria-current="page" asp-route="@RouteConstants.PrimaryAcademicPerformanceMeetingOrExceedingStandards" asp-all-route-data="@Model.RouteAttributes">@PrimarySchoolPageTitles.MeetingOrExceedingStandards</a>
                </li>
                <li class="moj-sub-navigation__item">
                    <a class="moj-sub-navigation__link" asp-route="@RouteConstants.PrimaryAcademicPerformanceSubjectScaledScores" asp-all-route-data="@Model.RouteAttributes">@PrimarySchoolPageTitles.SubjectScaledScores</a>
                </li>
                <li class="moj-sub-navigation__item">
                    <a class="moj-sub-navigation__link" asp-route="@RouteConstants.PrimaryAcademicPerformanceAdditionalMeasures" asp-all-route-data="@Model.RouteAttributes">@PrimarySchoolPageTitles.AdditionalMeasures</a>
                </li>
            </ul>
        </nav>

        <h2 class="govuk-heading-l">Meeting or exceeding expected standards for the previous academic year</h2>

        <p class="govuk-body">Information in this section is for the 2024 to 2025 academic year. </p>
        <p class="govuk-body">
            These measures show the percentage of pupils achieving the expected or higher standard based on assessments at the end of key stage 2 typically when they are aged 10 or 11 and in year 6.
        </p>
        <p class="govuk-body">
            Pupils sit national tests in English reading, maths and grammar, punctuation and spelling. In writing, teachers asses which level pupils are working at.
        </p>


        <div class="data-container">

            <h3 class="govuk-heading-m govuk-heading-m govuk-!-margin-top-3">
                Meeting expected standard in reading, writing and maths
            </h3>
            <p class="govuk-body">
                Pupils achieve the expected standard when they:
            </p>
            <ul>
                <li>achieve a scaled score of 100 or higher in reading and maths</li>
                <li>are assessed by their teacher as working at the expected standard or greater depth for writing</li>
            </ul>

            <p class="govuk-body">
                Compare this school with state-funded local and national averages.
            </p>

            <input type="radio" name="dataView" id="current-view" class="govuk-visually-hidden dataview-button" aria-labelledby="all-mes-show-current-data-btn" checked />
            <input type="radio" name="dataView" id="data-overtime-view" class="govuk-visually-hidden dataview-button" aria-labelledby="all-mes-show-data-over-time-btn" />

            <div id="all-mes-current-year-data-container" class="all-mes-current-year-data-container">
                <div id="all-mes-current-year-chart-container" class="chart-container all-mes-current-year-chart-container">
                    <canvas id="all-mes-data-current-year-chart"
                            class="js-chart"
                            data-type="bar"
                            data-chart='@Html.Raw(Json.Serialize(Model.AllMeetingExceedingStandardsData))'
                            data-colors='@Html.Raw(Json.Serialize(Constants.ChartColours))'>
                    </canvas>
                </div>

                <div id="all-mes-current-year-table-container" class="all-mes-current-year-table-container">
                    <table id="all-mes-current-year-table" class="govuk-table">
                        <tbody class="govuk-table__body">
                            @foreach (var label in Model.AllMeetingExceedingStandardsData.Labels.Select((value, i) => (value, i)))
                            {
                                <tr class="govuk-table__row">
                                    <th scope="row" class="govuk-table__header">@label.value</th>
                                    <td class="govuk-table__cell">@Model.AllMeetingExceedingStandardsData.Data[label.i]%</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <div class="govuk-button-group">
                    <govuk-button id="all-mes-current-year-show-btn" type="button" class="govuk-button--secondary all-mes-current-year-show-btn">Show as a table</govuk-button>
                </div>

                <div class="govuk-button-group">
                    <label id="all-mes-show-data-over-time-btn" for="data-overtime-view" class="govuk-button govuk-button--secondary" tabindex="0">Show data over time</label>
                </div>
            </div>



            <div id="all-mes-data-over-time-data-container" class="all-mes-data-over-time-data-container">
                <div id="all-mes-data-over-time-chart-container" class="chart-container all-mes-data-over-time-chart-container">
                    <div id="all-mes-data-overtime-chart-legend" class="chart-legend" data-chart-id="all-mes-data-overtime-chart"></div>
                    <div class="chart-container stacked">
                        <canvas id="all-mes-data-overtime-chart"
                                class="js-chart"
                                data-type="bar"
                                data-show-legend="true"
                                data-chart='@Html.Raw(Json.Serialize(Model.AllMeetingExceedingStandardsOverTimeData))'
                                data-colors='@Html.Raw(Json.Serialize(Constants.ChartColours))'>
                        </canvas>
                    </div>
                </div>

                <div id="all-mes-data-over-time-table-container" class="all-mes-data-over-time-table-container">
                    <table id="all-mes-data-overtime-table" class="govuk-table">
                        <thead class="govuk-table__head">
                            <tr class="govuk-table__row">
                                <th scope="col" class="govuk-table__header"></th>
                                @foreach (var label in Model.AllMeetingExceedingStandardsOverTimeData.Labels)
                                {
                                    <th scope="col" class="govuk-table__header">@label</th>
                                }
                            </tr>
                        </thead>
                        <tbody class="govuk-table__body">
                            @foreach (var dataset in Model.AllMeetingExceedingStandardsOverTimeData.Datasets.Select((value, i) => (value, i)))
                            {
                                <tr class="govuk-table__row">
                                    <th scope="row" class="govuk-table__header">@dataset.value.Label</th>
                                    @foreach (var data in dataset.value.Data)
                                    {
                                        <td class="govuk-table__cell">@(data.HasValue ? data + "%" : Constants.NotAvailable)</td>
                                    }
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <div class="govuk-button-group">
                    <govuk-button id="all-mes-data-over-time-show-btn" type="button" class="govuk-button--secondary all-mes-data-over-time-show-btn">Show as a table</govuk-button>
                </div>

                <div class="govuk-button-group">
                    <label id="all-mes-show-current-data-btn" for="current-view" class="govuk-button govuk-button--secondary" tabindex="0">Show current data</label>
                </div>

            </div>
        </div>

        <govuk-pagination id="bottom-pagination">
            <govuk-pagination-previous asp-route="@RouteConstants.PrimaryAcademicPerformancePupilProgress" asp-all-route-data="@Model.RouteAttributes">Primary academic performance: Pupil progress</govuk-pagination-previous>
            @if (Model.IsKS4)
            {
                <govuk-pagination-next asp-route="@RouteConstants.SecondaryAcademicPerformanceAttainmentAndProgress" asp-all-route-data="@Model.RouteAttributes">Secondary academic performance: Progress and attainment</govuk-pagination-next>
            }
        </govuk-pagination>
    </div>
</div>

            
@section Scripts {
    <script src="~/js/chart.umd.js"></script>
    <script src="~/js/chartjs-plugin-datalabels.min.js"></script>
    <script src="~/js/chart-factory.js"></script>
    <script src="~/js/meeting-exceeding-standards.js"></script>
}