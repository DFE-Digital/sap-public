apiVersion: apps/v1
kind: Deployment
metadata:
  name: sap-public-maintenance
  labels:
    app: sap-public-maintenance
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sap-public-maintenance
  template:
    metadata:
      labels:
        app: sap-public-maintenance
    spec:
      nodeSelector:
        teacherservices.cloud/node_pool: applications
        kubernetes.io/os: linux
      containers:
      - name: sap-public-maintenance
        image: ghcr.io/dfe-digital/sap-public-maintenance:#MAINTENANCE_IMAGE_TAG#
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 64M
            cpu: 0.1
          limits:
            memory: 64M
            cpu: 1
