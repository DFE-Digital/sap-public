apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test.schoolprofiles.education.gov.uk
spec:
  ingressClassName: nginx
  rules:
  - host: test.schoolprofiles.education.gov.uk
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: sap-public-test
            port:
              number: 80
