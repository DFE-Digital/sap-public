apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sap-public-temp.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: sap-public-temp.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: sap-public-test
            port:
              number: 80
